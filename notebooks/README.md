# Notebook Guide

Use `collectibles_investment_analysis.ipynb` as the supported, end-to-end analysis entry point. It loads the committed return snapshots, evaluates risk-adjusted performance, and generates the comparison outputs.

The repository previously contained empty phase-notebook placeholders. They have been removed to avoid presenting invalid notebook files as runnable analysis steps.
